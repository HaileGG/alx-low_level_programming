#include "main.h"
#include <stdio.h>

/**
 * main - prints the name of the program.
 * @argc: Number of command line arggumets.
 * @argv: Name of Array.
 * Return: 0
 */
int main(int argc, char argv[])
{
	printf("%s", argv[argc * 0]);

	return (0);
}
